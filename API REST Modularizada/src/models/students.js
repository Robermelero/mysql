class Student {

    constructor(student_id, first_name, last_name, group_id, year){
                this.student_id = student_id,
                this.first_name= first_name,
                this.last_name= last_name,
                this.group_id= group_id,
                this.year= year
}
};



module.exports = {Student};